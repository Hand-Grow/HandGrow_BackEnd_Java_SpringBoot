spring:
  application:
    name: demo

  # ===============================
  # 1. REDIS CONFIGURATION
  # ===============================
  data:
    redis:
#      host: localhost
#      port: 6379
#      password: your_password
#      database: 0
#      timeout: 60000ms

  # ===============================
  # 2. DATABASE CONFIGURATION
  # ===============================
  datasource:
    url: jdbc:postgresql://aws-1-ap-southeast-1.pooler.supabase.com:6543/postgres?sslmode=require
    username: postgres.lchqmvwbreujihzggwkj
    password: lapxuongnuongda
    driver-class-name: org.postgresql.Driver
    hikari:
      minimum-idle: 5
      maximum-pool-size: 20
      idle-timeout: 30000
      max-lifetime: 1800000

  # ===============================
  # 3. JPA CONFIGURATION
  # ===============================
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
#        format_sql: true


# ===============================
# 4. CUSTOM JWT CONFIGURATION
# ===============================
application:
  security:
    jwt:
      secret-key: "d09bd52f9ffc6efe95fa6499197c3a093b998a593b1b99a1c009b254e3bfbde3"
      expiration: 86400000
      refresh-token:
        expiration: 604800000

# ===============================
management:
  health:
    redis:
      enabled: false